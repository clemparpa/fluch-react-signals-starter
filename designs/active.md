# Active design

Placeholder file. The `apply-theme` skill (and any future design-system tooling) will overwrite this document with the currently active visual design — tokens, colors, typography, spacing, component variants — so that the AI agent can read it as a single source of truth before generating UI.

Until a design is applied, the project uses the default **shadcn (Base UI variant)** look-and-feel defined in [src/index.css](../src/index.css) (Tailwind v4 `@theme` tokens in oklch).
