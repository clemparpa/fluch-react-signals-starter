# This template does not ship an auth server.
# Point this at a running better-auth backend (e.g. your own server,
# the fluch backend template, or any compatible instance).
VITE_AUTH_BASE_URL=http://localhost:8000
